name: Build SilverBullet COMPLETE PACK

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Setup NuGet
        uses: nuget/setup-nuget@v2

      # Cài đặt môi trường .NET Framework cần thiết
      - name: Install .NET Framework 4.7.2
        run: choco install netfx-4.7.2-devpack -y

      # Tìm file .sln của SilverBullet
      - name: Find Solution
        shell: pwsh
        run: |
          Write-Host "🔍 Searching for .sln file..."
          $sln = Get-ChildItem -Recurse -Filter *.sln | Select-Object -First 1
          if (-not $sln) {
            Write-Error "❌ Không tìm thấy file .sln!"
            exit 1
          }
          Write-Host "✅ Found: $($sln.FullName)"
          "SLN=$($sln.FullName)" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8

      # Restore thư viện NuGet
      - name: Restore
        shell: pwsh
        run: |
          Write-Host "📦 Restoring packages..."
          nuget restore "$env:SLN"

      # Build toàn bộ Solution (Bao gồm OpenBullet UI và CLI)
      - name: Build
        shell: pwsh
        run: |
          Write-Host "⚙️ Building SilverBullet (OpenBullet)..."
          msbuild "$env:SLN" /p:Configuration=Release /p:Platform="Any CPU" /verbosity:minimal
          exit 0

      # 📤 HỐT TRỌN THÀNH PHẨM (Tập trung vào file chạy .exe)
      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: SilverBullet-Full-Package
          path: |
            **/bin/Release/
            **/*.exe
            **/*.dll
            **/*.config
            **/*.json
          if-no-files-found: warn
